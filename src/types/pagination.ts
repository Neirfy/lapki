export type PaginationInfo = {
    total: number
    previos: number
    page: number
    next: number
    per_page: number
    total_pages: number
  }

export type PaginationFilter = {
  page: number
  per_page: number
}
